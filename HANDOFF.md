# Handoff — White One ERP

> Передача дел новому чату. Последняя сессия: **28 мая 2026 (утро, переезд на Opus 4.8)**.
> До этого: PR #127 «мобильная адаптация», PR #129 «Цели сезона».
> Эта сессия — PR #132–#137 (чек-боксы, DnD, агрегация комментов, упаковка на канбане,
> перенос меню, redeploy).

## ⚡ Сессия 27-28 мая — улучшения после мобильной адаптации (PR #132–#137)

**В проде (все смержены, последний redeploy #137 триггерит свежий build):**

- **#132** — Чек-боксы задач на /dashboard с inline date picker.
  Алёна: «надо было 2 дня назад принять партию, про неё все забыли, задача
  висит». Теперь возле каждой задачи в зоне «Сейчас» — чек-бокс. Тык →
  inline-кнопки [Сегодня] [Вчера] [-2 дн] [-3 дн] (+ «-N дн (по плану)»
  если задача просрочена больше 3 дней). Жмём дату — server action
  обновляет реальные поля БД:
    order-qc        → Order.status = QC
    accept-qc       → status = READY_SHIP + qcDate (если null)
    check-delivery  → status = WAREHOUSE_MSK + arrivalActualDate
    size-chart      → ProductModel.sizeChartReady = true
    approve-sample  → ProductModel.status = APPROVED
    pkg-check-delivery → status = ARRIVED + arrivedDate
  Остальные kind'ы (order-sample, start-production, pkg-launch и т.п.) без
  чек-бокса — клик ведёт в карточку, потому что они требуют создания
  сущности (заказ/PO).
  Файлы: `src/app/(app)/dashboard/{actions.ts,checkable-row.tsx,checkable-kinds.ts}`.

- **#133** — «Цели сезонов» → «Цели». В основной таб мобилы (Главный · Цели ·
  Заказы · Каталог · Ещё). «Упаковка» переехала в «Ещё». Описание ужато
  до одной строки.

- **#134** — DnD-зона в комментариях стала явной: пунктирная рамка по
  умолчанию + подсказка «📎 Перетащите фото, вставьте из буфера (⌘V) или
  нажмите». Добавлена поддержка paste из буфера (Cmd/Ctrl+V) — для
  скриншотов.

- **#135** — Агрегация комментариев + перенос «Планирования работы».
  API /api/comments расширен:
    • entityType="variant" теперь разрешён.
    • Новый флаг includeRelated=1 (legacy includeOrders продолжает работать).
    • На фасоне → подтягиваются комменты заказов + вариантов.
    • На заказе → подтягиваются комменты родительского фасона.
    • contextLabel: "ORD-...", "цв. молочный", "к фасону".
  Sidebar: «Планирование работы» (/admin/plans) перенесена из «Справочников»
  в основной NAV под «План/Факт».

- **#136** — Заказы упаковки на канбане /models/kanban.
  PackagingOrder теперь рендерится отдельными карточками в тех же колонках
  post_order/done. Колонка «ОТК» пропускается — у упаковки нет фазы ОТК.
  Drag-n-drop для упаковки отключён (статусы меняются в карточке).
  Карточка: 📦 PKG-XXXX · название первой PackagingItem, фото первой
  позиции, общее кол-во шт, дедлайн с цветом риска. Категория «Упаковка»
  появилась в фильтре сверху.
  Расширение: `KanbanCard.kind?: "model" | "packaging-order"`.

- **#131, #137** — Empty redeploy. Vercel cancels previous queued production
  builds, когда новые PR мержатся подряд — серия PR может зависнуть. Если
  на проде задержка вернётся — пустой коммит с PR триггерит redeploy.

### ⚡ Что важно для нового окна

1. **На дашборде задачи закрываются «галкой» только если они однозначные.**
   `isCheckable(kind)` в `dashboard/checkable-kinds.ts` — добавь сюда новый
   kind, если для него очевидно поле БД для закрытия.

2. **Сезонные константы в коде** (`src/lib/seasons.ts`), не в БД. Если Алёна
   попросит «давай ещё сезон» — обновить SEASONS массив.

3. **`MonthlyPlan.unique(yearMonth, ownerId, category)`** — null != null в
   Postgres, поэтому категорийные шаблоны (ownerId=null) и план PM
   (category=null) не пересекаются.

4. **Авторский маркер в notes** `'Сгенерирован автоматически (цель 10/20 000
   на PM)'` — единственный способ отличить автосгенерированные планы от
   ручных. Кнопка «Очистить» на /seasons удаляет ТОЛЬКО по этому маркеру.

5. **Vercel queue race** — если делаешь несколько PR подряд, мержи их с паузой
   2-3 минуты или сделай empty commit в конце для гарантированного redeploy.

## ⚡ Сессия 27 мая, ночь — Цели сезона (PR #129)

## ⚡ Сессия 27 мая, ночь — Цели сезона (PR #129)

**В проде:**
- **#129** — Новый экран **/seasons** для руководителя продукт-отдела.
  Одним взглядом видно где компания относительно цели «10 артикулов и
  20 000 шт в месяц» по сезонам до конца года.

  **Сезоны (`src/lib/seasons.ts`):**
  - Лето (июн-авг) — Летние платья, Летние костюмы, Блузки
  - Осень (сен-ноя) — Пальто, Полупальто, Джинсы, Брюки, Трикотажные костюмы
  - Зима (дек) — Пальто, Полупальто, Трикотажные костюмы

  **Структура страницы:**
  - Табы сезонов со счётчиком факта-к-цели в %
  - Сезонная сводка: цель / план / факт с двойным баром
  - Нагрузка по месяцам: 🟢/🟡/🔴/⚪/📅 по статусу
  - Разрез по PM с прогресс-баром штук и чипом статуса
  - Разрез по 8 категориям через нечёткое `matchSeasonCategory()`
  - Заторы: фасоны не в производстве + заказы PREPARATION/FABRIC_ORDERED
    с launchMonth ≤ 45 дн

  **Генератор плана (server action `fillDefaultPlan2026`):**
  - Заполняет пустые слоты MonthlyPlan на остаток года цифрами 10/20 000
  - Поровну между активными PRODUCT_MANAGER (OWNER не несёт KPI)
  - Идемпотентно, маркер в notes для последующей очистки
  - Парная кнопка «Очистить» удаляет только эти автосгенерированные записи

  **Навигация:** /seasons в сайдбаре (десктоп) и в «Ещё» (мобиле).

## ⚡ Сессия 27 мая, ночь — мобильная адаптация (PR #127)

**В проде:**
- **#127** — мобильная адаптация шести экранов на iPhone 390×844:
  - **/gantt-v2** — на мобиле нативный Гант: карточка заказа с фазовым баром
    собственной шкалы + статус-строкой («► Доставка · до 28.05 (1 дн)» /
    «🔥 ОТК просрочен на 3 дн») + раскрытие с детальными фазами и кнопкой
    «Открыть карточку». Десктоп с горизонтальным Гантом не тронут.
  - **/dashboard** — `getMainScreenChecklist` расширен: подтягивает разработку
    упаковки (PackagingItem lifecycle IDEA→DESIGN→SAMPLE→APPROVED→ACTIVE) и
    заказы упаковки в пути (PackagingOrder IN_PRODUCTION/IN_TRANSIT). Новая
    секция «Упаковка» рядом с «Заказы» и «Разработка». kind'ы: `pkg-design`,
    `pkg-sample`, `pkg-approve`, `pkg-launch`, `pkg-check-delivery`.
  - **/plan-vs-fact** — на мобиле таблица 4 колонок (с переносами заголовков в
    3 строки) превратилась в карточки с прогресс-барами фасонов/штук.
  - **/payments** — на мобиле в режиме «Календарь» вместо сетки 7×N рендерится
    лента «по дням месяца» с карточками платежей; десктопный календарь цел.
  - **/orders/[id]** — KPI «Себестоимость партии» сверху во всю ширину +
    «Шт/Кол-во» снизу в одной строке (3 в ряд на десктопе). Таблица позиций
    по размерам переносится под названием цвета на полную ширину карточки
    (раньше 6 размеров слипались в `11155`).
  - **/models/kanban** — на мобиле pill-табы по колонкам со счётчиками,
    выбранная колонка во всю ширину; десктопная доска с 8 колонками цела.

## ⚡ Сессия 26-27 мая, вечер (PR #116-#125)

### В проде
- **#117** — Канбан: убран переключатель «Список / Канбан» сверху (дубль сайдбара)
- **#118** — Сайдбар: «Ещё» → «Смежные отделы». Платежи переехали туда, Фабрики в Справочники, План/Факт в основной
- **#119** — «Главный» = `/dashboard` (чек-лист). Старая `/my-tasks` удалена целиком (page.tsx + lib/queries/my-tasks.ts). «План продаж» → «Планирование работы»
- **#120** — Канбан «Завершено»: фото grayscale + opacity-80
- **#121** — Заказы: «Старт продаж» удалён, добавлена «Сумма заказа» (Σ по линиям, fallback на fullCost фасона). Платежи: убрано серое выходных в календаре
- **#122** — Комменты v2: удаление (автор/admin), фото DnD+upload, миксованный поток на /models/[id] (фасон+комменты заказов с меткой ORD-XXXX). DELETE /api/comments/[id], GET ?includeOrders=1
- **#123** — Гант: автоскролл scrollLeft=0 при заходе (левая колонка не прячется). В idle-задачах разработки показывается статус фасона + N дн без движения. SLA approve-sample 21→14дн.
- **#124** — Заказы: колонка «Упаковка» — названия PackagingItem через запятую
- **#125** — Поставки: ⤓ XLSX выгрузка (лист «План отгрузок» + лист «По артикулам и размерам»)

### Главный остающийся фолловап — критично
**Переписать таймлайн в форме создания/редактирования заказа на движок /gantt-v2.**
Сейчас `src/components/orders/order-timeline.tsx` (Timeline на 4 фазы Разработка/Производство/ОТК/Доставка)
визуально болеет в форме: маленький контейнер, плашки наезжают, метки обрезаны.
Алёна: «представь лицо продакт менеджера который сунулся заполнить эту таблицу».
Использовать те же приёмы что в `/gantt-v2`:
- pixel-scale + horizontal scroll (а не проценты от viewport)
- ◀▶ drag по краям (контракт жестов из memory feedback_gantt_drag_edges.md)
- Цвета slate/blue/amber/emerald
- Поддержка hasFactoryQc=false → 3 фазы вместо 4 (для соответствующих заказов)

### Согласованные принципы (в Claude memory)
- `project_white_one_checklists.md` — 7 типов задач, подвкладки по PM
- `project_white_one_order_lifecycle.md` — архив только по клику «Прибыло», колонка «Завершено» только дата, `hasFactoryQc` дефолт true
- `feedback_result_over_process.md` — задачи закрываются по результату (смена статуса/даты в БД), не по кнопке. Чек-лист — read-only индикатор.
- `feedback_gantt_drag_edges.md` — контракт ◀ первой = старт, ▶ любой = длительность; никаких clamp'ов

### Прод-миграции
✅ Применена миграция `20260527090000_add_comment_model` (Comment table) — точечно через psql.
⏳ Миграция `20260526180000_add_order_has_factory_qc` всё ещё ОТКРЫТА (PR #110 в open, не мержен — Алёна откладывала). На прод НЕ применена. Когда захочет — миграция тривиальная (одна колонка BOOLEAN DEFAULT true).

## ⚡ Сессия 26 мая 2026 — продолжение (PR #108-#115)

### В проде
- **#108** — Чек-лист «Главного» разделён на «Заказы» (типы 5-7) и «Разработка» (1-4) внутри подвкладки PM
- **#109** — Шапка Ганта: подписи «Пн 04.05» вместо «04.05» (зум 1м/3м)
- **#111** — Канбан: колонка «Завершено» вместо «В продаже». Падают **заказы** (не фасоны) после WAREHOUSE_MSK/PACKING/SHIPPED_WB/ON_SALE. На карточке только дата прибытия. У одного фасона с активным+завершённым заказом — две карточки в разных колонках.
- **#112** — Чек-лист расширен с 5 до 14 дн, три зоны: «Сейчас» (≤2дн+просрочка+idle), «На неделе» (3-7), «Следующая неделя» (8-14, приглушённый). Внутри каждой зоны разбивка Заказы/Разработка.
- **#113** — SLA для задач разработки (proxy через `ProductModel.updatedAt`): order-sample 14дн, approve-sample 21дн, size-chart 7дн, start-production 7дн. Превышено → задача попадает в «Сейчас» как overdue с пометкой `· N дн без движения`. Визуальное старение: левая рамка задачи цветеет 8/22/45 дн → жёлтая/оранжевая/красная. Счётчик «в разработке >30 дн: N» сверху раздела.
- **#114** — Зелёный мини-баннер «✓ Закрыто в N: X заказов» над зонами по выбранной подвкладке PM. Считается по `arrivalActualDate` в текущем месяце МСК.

### Открытые PR — требуют миграции на проде
**Без `prisma migrate deploy` НЕ МЕРЖИТЬ** — сломают прод (отсутствуют колонки/таблицы).

- **#110** `feat/order-has-factory-qc` — поле `Order.hasFactoryQc Boolean @default(true)`. Чекбокс «Фабрика проводит ОТК» в форме edit. Чек-лист пропускает задачи 5/6 для заказов с `false`. Миграция: `20260526180000_add_order_has_factory_qc`.
- **#115** `feat/comments-mvp` — модель Comment с полиморфной привязкой (`entityType: model|order`, `entityId`). API `/api/comments` GET+POST. Компонент `CommentsThread` подключён на `/orders/[id]`. Миграция: `20260527090000_add_comment_model`.

### Согласованные принципы (в Claude memory)
- `project_white_one_checklists.md` — 7 типов задач, подвкладки по PM
- `project_white_one_order_lifecycle.md` — архив только по клику «Прибыло», колонка «Завершено» только дата, `hasFactoryQc` дефолт true, без фабричного ОТК нам пофиг что дальше происходит на складе
- `feedback_result_over_process.md` — задачи закрываются по результату (смена статуса/даты в БД), не по нажатию кнопки. **Чек-лист — read-only индикатор, никаких кнопок «выполнено» на задачах.**

### Фолловапы (для следующей сессии)
1. **Применить миграции на проде** и смержить PR #110, #115
2. Глубокая правка для `hasFactoryQc=false`: таймлайн формы заказа (3 фазы вместо 4), Гант v2 (3 плашки), форма создания заказа
3. Подключить CommentsThread на `/models/[id]` с миксованным потоком (фасон + комменты заказов с меткой)
4. Счётчик «💬 N» на карточках канбана
5. Фото drag-n-drop в комментариях, edit/soft-delete контролы
6. Тост «✓ Заказ X завершён» + микроанимация улёта карточки при смене статуса на WAREHOUSE_MSK
7. Поле `ProductModel.statusChangedAt` (вместо proxy через `updatedAt`) для точного SLA

---

> Прежняя часть этой же сессии: чек-лист 7 типов задач (PR #107).

## ⚡ Сессия 26 мая 2026 — главный экран как чек-лист

### Что выкатили (PR #107)

- **`/dashboard` полностью переписан**: убраны платежи, разрыв плана, дефицит упаковки, КПИ. Остался только чек-лист задач отдела продукта по 7 триггерам:
  1. Закажите образец — фасон IDEA без живого заказа
  2. Утвердите идеальный образец — PATTERNS/SAMPLE без живого заказа
  3. Сделайте размерную сетку — APPROVED + `sizeChartReady=false`
  4. Запустите производство — APPROVED + `sizeChartReady=true` без живого заказа
  5. Закажите ОТК — Order.SEWING + `readyAtFactoryDate` ≤ 5 дн (или просрочено)
  6. Примите ОТК — Order.QC/READY_SHIP + `qcDate` ≤ 5 дн (или просрочено)
  7. Проверьте доставку — Order.IN_TRANSIT + `arrivalPlannedDate` ≤ 5 дн (или просрочено)
- **Подвкладки динамические** — по всем PM с активными задачами, в URL `?owner=<userId>`. Не-админ видит только свою.
- **Гибрид-сортировка для задач 1-4**: есть `plannedLaunchMonth` → дедлайн считается с буфером (90/75/60/50 дней до 1-го числа), применяется порог 5 дн; нет launchMonth → задача попадает в раздел «Давно не двигалось», сортируется по `updatedAt`.
- **Эмодзи срочности**: 🔴 просрочено (красный текст), 🟡 ≤2 дн, ⚪ ≤5 дн. На каждой плашке — статусная точка.
- **Логика чек-листа в shared lib** — `src/lib/queries/main-screen-checklist.ts` + `groupByOwner`. Превью-скрипт `scripts/checklist-preview.ts` использует те же триггеры (но порог 7 дн, оставлен для DEV-аудита).

### Архитектурные заметки

- На проде 0 фасонов и 0 заказов без `ownerId` — это инвариант. Если появится — задача исчезнет из чек-листа автоматически, но проверять руками.
- Старая логика `getMyTasks` в `src/lib/queries/my-tasks.ts` НЕ удалена — `/my-tasks` продолжает работать как было. Если со временем не нужна — можно убрать.
- `moscowToday()` дублируется в `gantt-v2/page.tsx` и теперь в `main-screen-checklist.ts`. Не пересохранила в `src/lib/` — отдельный мелкий рефактор.

### Открытые вопросы на следующую сессию

- Стоит ли иконизировать тип задачи (образец / ОТК / доставка) — сейчас всё в тексте.
- Нужно ли разделить idle-секцию по типу (отдельно «закажите образец», отдельно «запустите производство»)? Сейчас плоский список по updatedAt.

---

> Прежняя сессия: **24–25 мая 2026**.
> Большая сессия: склад, импорт партий, упрощение себестоимости,
> переписан таймлайн заказа на pixel-scale, drag-clamp, фильтры в стиле Ганта
> на /orders и /models/kanban. Все PR'ы #72–89 в проде.

## ⚡ Сессия 24-25 мая 2026 — краткая справка

### Что выкатили (PR'ы 72-89)

**Новые страницы / возможности:**
- `/warehouse` (Склад) + экспорт XLSX — `1 строка = 1 заказ`, ссылка на фото абсолютная HTTPS, для внешней складской системы (#72)
- На канбане фасонов: фильтры в стиле Ганта (Категория · Ответственный · Производство) (#87)
- На `/orders`: те же фильтры (#78)
- В Ганте: новый фильтр **Производство** (Россия / Китай / Тяк — по `factory.name` и `country`) (#81)
- Hover на превью в Ганте → увеличение ×4 (#86)
- Разлиновка Ганта по дням (zoom 1м) + зебра выходных (#86)

**Архитектурные решения:**
- Гант v2 теперь **основной** «График Ганта». `/gantt` → редирект на `/gantt-v2` (#81)
- Старая ссылка «Гант» убрана из сайдбара
- **Единый компонент `FilterDropdown`** (`src/components/common/filter-dropdown.tsx`) — переиспользуется на ганте, /orders, /models/kanban
- **Себестоимость фасона = ОДНО поле** (`purchasePriceRub` или `purchasePriceCny` + переключатель валюты). Старые `targetCost*` остались как legacy fallback, при первом редактировании мигрируются (#83, #85)
- Автоподтяг в заказ: при создании заказа стоимость и фабрика тянутся из фасона (с возможностью переопределить) (#83)
- Backfill себестоимости для линий теперь учитывает всю цепочку: `fullCost > purchasePriceRub > purchasePriceCny×rate > targetCostRub > targetCostCny×rate` (#89)

**Таймлайн в форме заказа/упаковки полностью переписан:**
- Был: проценты от viewport → плашки прыгали, min-width ломал пропорции
- Стал: **pixel-scale + horizontal scroll**, день имеет фикс. ширину (px/день по зуму), длинный цикл прокручивается (#84)
- Drag-логика жёсткая: `pxPerDay = dayWidth`, нет зависимости от viewport
- Визуально подтянут к /gantt-v2: тонкие вертикальные ручки 3px, hover-only, цвета slate/blue/amber/emerald (#80)
- Авто-расчёт фаз: **фиксированные длительности** (Разработка 14дн → Производство мин 30дн → ОТК 5дн → Доставка по способу), а не пропорции 15/70/15 (#82)
- Шкала охватывает min/max ВСЕХ фаз (фикс перепутанных min/max в reduce — был критический баг рендера) (#82)
- Min-width плашки 64px + 4px gap между фазами → ручки ◀▶ не наезжают (#79, #84)

**Drag-overlap фикс в Ганте:**
- В каскаде сдвига хвоста теперь пост-clamp: end следующей фазы ≥ её start. Фазы не накладываются даже при сильном drag (#87)

**Канбан логика:**
- Фасон в IDEA / PATTERNS / SAMPLE не уезжает в колонку заказа — колонка по статусу фасона (#88). Раньше любой заказ перекрывал.

**Категории:**
- Добавлены «Костюмы» и «Сарафаны» в `CATEGORIES` (`src/lib/constants.ts`) (#75)

**Импорт партий (через scripts/, не PR, разово на проде):**
- 9 заказов от ORD-2026-0034 до ORD-2026-0042 (партии 1 и 2): Костюм двойка, Брюки атласные бесшовные, Платье ципао, Костюм двойка полоска, Брюки алладины, Сарафан летний античный, Сарафан с воланами, Костюм двойка Асель (ONE SIZE), Костюм летний с шортами
- Созданы новые SizeGrid: `S-XL (4)`, `ONE SIZE`

### Открытые таски на следующую сессию

1. **10 заказов без себестоимости в фасоне** — после backfill (#89) остались с прочерком, потому что у их фасонов вообще не вписана цена:
   `ORD-2026-0008 (Полупальто стойка), 0009 (Кроп пальто), 0024/0027 (Платье Воздушное), 0025/0029 (Платье Кимоно), 0030 (Платье крылья), 0032 (Брюки Love Republic), 0033 (Брюки Массимо), 0037 (Костюм двойка полоска)`.
   Алёна должна руками вписать цену на странице каждого фасона. После этого backfill подтянет автоматически при открытии заказа.

2. **ORD-2026-0037 «Костюм двойка полоска» — нелогичный порядок фаз в БД** (qcDate < readyAtFactoryDate или похожее). На Ганте видна как наложение синей плашки. Пост-clamp #87 защищает от создания таких в будущем, но старые данные не починены. План: одноразовый скрипт-миграция, выравнивающий цепочку дат для всех заказов с `hasDateOrderIssue=true`.

3. **«Брюки Массимо» (ORD-2026-0033)**: статус заказа уже откатили SEWING → PREPARATION (фасон ещё в разработке). Алёна попросила — сделано прямой правкой в БД.

### Архитектурные заметки для нового чата

- **Подтяг себестоимости** на странице заказа `/orders/[id]` использует `modelFullCost` с цепочкой (см. `src/app/(app)/orders/[id]/page.tsx`). На форме создания — функция `modelDefaultUnitCost` в `order-form.tsx`. На уровне БД — `backfillOrderEconomicsFromModel`. **Три места**, все три обновлены с одинаковой логикой.
- **FilterDropdown** общий: `src/components/common/filter-dropdown.tsx`. При добавлении нового фильтра — переиспользуй.
- **Таймлайн в форме** (`order-timeline.tsx`, `packaging-order-timeline.tsx`) и **таймлайн на ганте** (`gantt-v2-chart.tsx`) — это **разные компоненты**, не общий код. Drag-логика похожая, но независимая.
- **Регион производства** считается через хелпер `productionRegionOf` — дублируется в `/gantt-v2/page.tsx` и `/models/kanban/page.tsx`. При следующем рефакторе можно вынести в `src/lib/`.
- **Категории** — фиксированный список в `constants.ts`. Если Алёна вписала фасон с категорией, которой нет в списке, в форме select подсветится пусто. Сейчас все используемые категории в списке.

### Запуск скриптов на проде

`.env.local` содержит `DATABASE_URL` от **Neon prod**. Запуск любого скрипта:
```bash
cd ~/projects/white-one
(set -a; source .env.local; set +a; npx tsx scripts/имя-скрипта.ts)
```

Артефакты последних импортов:
- `scripts/import-batch-2026-10.ts` — партия 1
- `scripts/import-batch-2026-10-part2.ts` — партия 2

---

## 📍 Где мы сейчас

- **Ветка**: `main` (всё что сделано — уже на проде)
- **Прод**: https://white-one-erp.vercel.app (Vercel автодеплой с push в main)
- **Логин Алёны**: `alena@whiteone.ru` (пароль в репозитории не хранится)
- **Команда**: 11 человек, Алёна (OWNER), Даша (DIRECTOR), Вера+Оля (PM, Оля в Гуанчжоу), Настя (ASSISTANT-склад), Катя (CONTENT), Таня (LOGISTICS), Элина (CUSTOMS), Вика+Лиза (WB)

## ✅ Что выкатили в эту сессию (PR'ы 35–51)

### /gantt-v2 — главный фронт работ

| PR | Что |
|---|---|
| [#35](https://github.com/vinogradova-for-ai/white-one-erp/pull/35) | Сплошная заливка плашек, убраны диагональные штрихи |
| [#36](https://github.com/vinogradova-for-ai/white-one-erp/pull/36) | Чистая шапка: только фильтры Категория+Ответственный, зум 1н/1м/3м. Плотность=normal по умолчанию (без переключателя). Сортировка по запуску ASC. Группировка убрана. Удалён wheel-хайджак (вертикальный скролл больше не превращается в горизонтальный) |
| [#39](https://github.com/vinogradova-for-ai/white-one-erp/pull/39) | Поле занимает h-[calc(100vh-200px)] — полная высота. Тултипы больше не накладываются (убран нативный `title`, остался только кастомный dark) |
| [#40](https://github.com/vinogradova-for-ai/white-one-erp/pull/40) | Палитра фаз: Разработка=slate-400, Производство=blue-500, ОТК=amber-500, **Доставка=emerald-500** (вместо fuchsia). Малиновый/розовый убраны целиком |
| [#44](https://github.com/vinogradova-for-ai/white-one-erp/pull/44) | Из тултипов убраны надписи «СКОРО ДЕДЛАЙН»/«ПРОСРОЧЕНО» — дубль с цветом обводки |
| [#45](https://github.com/vinogradova-for-ai/white-one-erp/pull/45) | Сортировка по дедлайну производства (end фазы Производство ASC), не по launchMonth |
| [#46](https://github.com/vinogradova-for-ai/white-one-erp/pull/46) | Убраны галочки ✓ на done-плашках |
| [#47](https://github.com/vinogradova-for-ai/white-one-erp/pull/47) | Убраны иконки 🔥/⚠️/↯даты из строк и легенда из шапки |
| [#48](https://github.com/vinogradova-for-ai/white-one-erp/pull/48) | Drag-resize левой колонки «Заказ/Фасон» (140–700px), сохранение в localStorage `gantt-v2:leftColWidth` |
| [#49](https://github.com/vinogradova-for-ai/white-one-erp/pull/49) | **Автосейв drag-изменений** через дебаунс 600мс с тостом «Сохранено» через sonner. Sticky save-bar убран целиком |
| [#50](https://github.com/vinogradova-for-ai/white-one-erp/pull/50) | Обводки только на активной фазе (отменено в #51) |
| [#51](https://github.com/vinogradova-for-ai/white-one-erp/pull/51) | **Все ring-обводки убраны** (red/amber). + todayIso по МСК (UTC+3) — иначе ночью 0–3 МСК «сегодня» уезжало на день назад |

### /gantt (старый)

| PR | Что |
|---|---|
| [#38](https://github.com/vinogradova-for-ai/white-one-erp/pull/38) | Убраны фантомные «Разработки» (done-фаза без обеих дат не рендерится). Заказы со статусом WAREHOUSE_MSK/PACKING/SHIPPED_WB помечены `archived` → уходят в конец списка, рендерятся с opacity-45 |
| [#51](https://github.com/vinogradova-for-ai/white-one-erp/pull/51) | todayIso по МСК (тот же фикс что и в gantt-v2) |

### /orders

| PR | Что |
|---|---|
| [#37](https://github.com/vinogradova-for-ai/white-one-erp/pull/37) | Добавлена строка пилюль «Ответственный» (Все/Алёна/Вера/Катя/Настя/Оля). Сортировка по launchMonth ASC, createdAt ASC (вместо updatedAt DESC) |

### Инфра

| PR | Что |
|---|---|
| [#29, #41, #42, #43](https://github.com/vinogradova-for-ai/white-one-erp/pulls) | Retry-deploys из-за Neon P1002 advisory lock. Известный transient timeout |

## 🎨 Текущая палитра фаз

- **Разработка** — `bg-slate-400` (серый, нейтральный)
- **Производство** — `bg-blue-500` (синий)
- **ОТК** — `bg-amber-500` (янтарный)
- **Доставка** — `bg-emerald-500` (зелёный)
- done-фаза → `opacity-50`
- активная и future → полный цвет
- **обводок нет** (ни на просроченных, ни на nearly-due)

## 🎯 Контракт жестов в Ганте (важно — Алёна явно сформулировала)

| Жест | Что меняется в БД |
|---|---|
| **◀ самой первой плашки** | startField (decisionDate) — все фазы сдвигаются на дельту |
| **▶ любой плашки** | endField этой фазы — соседи СПРАВА едут с сохранением длительностей |
| **◀ не первой плашки** | endField предыдущей фазы (= то же поле что её start) |

**Drag только за края (◀ ▶), не за середину.** Никаких clamp'ов.

## 💾 Автосейв (новое в этой сессии)

В `gantt-v2-client.tsx`:
- `handleBarChange` → буферит изменение + сбрасывает debounce-таймер на 600мс
- По таймеру `flushAutosave()` → батчит изменения по orderId → PATCH `/api/orders/[id]` или `/api/packaging-orders/[id]`
- Успех → `toast.success("Сохранено")` через sonner, `router.refresh()`
- Ошибка → `toast.error(...)`
- Sticky save-bar **удалён** — никакой кнопки «Сохранить», всё автоматом

## 📂 Где живёт логика Ганта v2

- `src/app/(app)/gantt-v2/page.tsx` — серверный data-fetcher. Генерирует `GanttRowV2[]` для заказов (4 фазы) и упаковки (3 фазы). `moscowToday()` для todayIso
- `src/components/gantt-v2/gantt-v2-client.tsx` — клиентский wrapper. Фильтрация, сортировка по дедлайну производства, автосейв
- `src/components/gantt-v2/gantt-v2-chart.tsx` — основной чарт, шкала, RowView, DraggableBar. Resize-handle левой колонки (`ResizeHandle`)
- `src/components/gantt-v2/types.ts` — типы

## 🔮 Что НЕ сделано — отдельный заход (большие фичи)

Эти два пункта были в исходном пакете правок 17.05, но это новые сущности с миграциями БД и формами CRUD — отложены до обсуждения модели данных:

### 1. Вкладка «Поступление»
Новая сущность (Receipt?), отдельная от Order:
- Список с датой поступления на склад, факт vs план по количеству
- Гант проверки и упаковки
- При создании — позиции автоматом из заказа, склад/ВЭД вписывают факт-количество
- **Открытый вопрос**: один заказ = много партий или одно поступление?

### 2. Автосчёт «тетрадка»
Таблица «цвет × размер × количество» по дням/доставкам с автосуммированием:
- **Открытый вопрос**: это часть Поступления (приёмка факт-количеств по цвет/размер)? Или отдельная сводная по нескольким заказам?

Алёна прикладывала фото тетради со столбцами «цвет / размер / количество» по дням — нужно автосчётно.

## ⚙️ Правила (memory + рабочие)

1. **Никаких `.default()`** в Zod-схемах с `.partial()` для PATCH — обнуляют поля
2. **Не пересоздавать БД** без явного консента (он действует — фейковые данные)
3. **Простой язык** для Алёны: «форма / справочник / карточка / ссылка», не «API / JWT / миграция»
4. **Короткие отчёты** после правок: «Готово. Обнови страницу. Теперь работает X.»
5. **Не переспрашивать про работу и деплой** — задача взяла → делаю → коммит → PR через `gh pr merge --squash --admin`. Push на main напрямую заблокирован политикой
6. **Никакой `git push --force` на main**
7. **Drag за края, не за середину** — главный UX-закон Гант'а
8. **Стиль Алёны**: скрин + короткая фраза → AI догадывается → чинит

## 🛠 Запуск локально

```bash
cd ~/projects/white-one
brew services start postgresql@16
npm run dev   # http://localhost:3000
```

## 📚 Доп ссылки

- `CONTEXT_FOR_NEW_SESSION.md` — большой контекст про продукт/архитектуру/статусы
- Memory: `~/.claude/projects/-Users-alenaleonteva-SecondBrain/memory/MEMORY.md`
- Vercel project: `white-one-erp` (org `vinogradova-for-ais-projects`)

## ⚠️ Известные особенности

- **Vercel preview-build** для PR'ов падает (ESLint? env?), prod build с main работает. Не блокирует мерж
- **Neon P1002** — periodic advisory lock timeout. Решается ретрай-PR'ом с пустым коммитом
- **localStorage `gantt-v2:leftColWidth`** — ширина левой колонки сохраняется на устройство пользователя

---

_Файл обновляется при передаче в новый чат. Актуально на 2026-05-18._
