-- READY_SHIP выпилен (Алёна 04.07: «нет отдельного статуса, только ОТК»).
-- Данные: живые заказы переводим в QC. Enum-значение остаётся (история логов),
-- из переходов статус недостижим (QC → IN_TRANSIT напрямую).
UPDATE "Order" SET status='QC' WHERE status='READY_SHIP';
