-- Списание упаковки при упаковке заказа (аудит п.7).
-- consumedQty: сколько штук упаковки списано со склада при переходе заказа в PACKING.
-- null — ещё не списывали (нужно для идемпотентности и отката при возврате статуса).
ALTER TABLE "OrderPackaging" ADD COLUMN "consumedQty" INTEGER;
